name: CI

# oracle-oci8-support is this fork's default branch, where the Oracle work lives;
# main only tracks upstream. Without it listed, no push to the fork's own branch
# and no pull request targeting it ever ran CI, so a PR merged with no checks at
# all. release.yml runs its own copy of these jobs on a tag, so a release was
# verified even while pull requests were not.
on:
  push:
    branches: ["oracle-oci8-support", "main", "release/**"]
  pull_request:
    branches: ["oracle-oci8-support", "main", "release/**"]

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: internal/ui/web/package-lock.json

      - name: Build UI
        run: make build-ui

      - name: Test UI
        run: make test-ui

      - name: Install tray build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --fix-missing libayatana-appindicator3-dev

      - name: Download dependencies
        run: go mod download

      - name: Build
        run: CGO_ENABLED=1 go build -o /dev/null ./cmd/lerd

      - name: Test
        run: CGO_ENABLED=1 go test ./...

      - name: Vet
        run: CGO_ENABLED=1 go vet ./...

      - name: Format
        run: test -z "$(gofmt -l .)"

  build-macos:
    name: Build & Test (macOS)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: internal/ui/web/package-lock.json

      - name: Build UI
        run: make build-ui

      - name: Download dependencies
        run: go mod download

      - name: Build (nogui)
        run: CGO_ENABLED=0 go build -tags nogui -o /dev/null ./cmd/lerd

      - name: Test
        run: CGO_ENABLED=0 go test -tags nogui ./...

      - name: Vet
        run: CGO_ENABLED=0 go vet -tags nogui ./...

      - name: Format
        run: test -z "$(gofmt -l .)"

  installer:
    name: Installer Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          git clone --depth 1 https://github.com/bats-core/bats-core.git /tmp/bats-core
          sudo /tmp/bats-core/install.sh /usr/local

      - name: Run installer tests
        run: bats tests/installer/installer.bats
