// Package desktopapp installs the clickable launcher that puts lerd in the
// platform's application list, so bringing the environment up and opening the
// dashboard needs no terminal.
package desktopapp

// Name is the launcher's user-facing name.
const Name = "Lerd"
